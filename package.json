{
  "name": "dc-decade",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "shared",
    "web"
  ],
  "scripts": {
    "codegen": "npm run codegen --workspace=web",
    "test": "npm test --workspace=web",
    "build": "npm run build --workspace=web",
    "dev": "npm run dev --workspace=web"
  },
  "devDependencies": {
    "yaml": "^2.8.2"
  }
}

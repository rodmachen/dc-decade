query GetSeriesWithCover($slug: String!, $offset: Int = 0) {
  seriesBySlug(slug: $slug) {
    id
    name
    slug
    yearBegan
    yearEnded
    issueCount
    publisher {
      id
      name
    }
    issues(limit: 5, offset: $offset) {
      id
      coverImageUrl
    }
  }
}

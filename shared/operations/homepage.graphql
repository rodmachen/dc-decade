query GetSeriesWithCover($slug: String!) {
  seriesBySlug(slug: $slug) {
    id
    name
    slug
    yearBegan
    yearEnded
    issueCount
    publisher {
      id
      name
    }
    issues(limit: 1) {
      id
      coverImageUrl
    }
  }
}

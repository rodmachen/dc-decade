query GetSeriesWithCover($id: Int!) {
  series(id: $id) {
    id
    name
    yearBegan
    yearEnded
    issueCount
    publisher {
      id
      name
    }
    issues(limit: 1) {
      id
      coverImageUrl
    }
  }
}
